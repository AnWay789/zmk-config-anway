CONFIG_ZMK_POINTING=y
CONFIG_I2C=y
CONFIG_GPIO=y
CONFIG_INPUT_PINNACLE=y

# Temporary debug: plug USB into the RIGHT half and run `tio /dev/cu.usbmodem*`
# Look for "Found device with FW ID" (OK) or "Failed to get the FW ID" (I2C wiring/R1).
CONFIG_ZMK_USB_LOGGING=y
CONFIG_LOG=y
CONFIG_ZMK_LOG_LEVEL_DBG=y
